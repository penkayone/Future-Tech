{
  "name": "Future-Tech",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watch": "sass --watch ."
  },
  "private": true,
  "devDependencies": {
    "sass": "^1.80.6"
  }
}
